projection implementation in class ZBP_C_12FLIGHT unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_12FLIGHT alias Flight
extensible
use etag
{

field ( readonly ) PlaneTypeId;
//  use create;
  use update;
//  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}